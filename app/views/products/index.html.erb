<h1>All Products</h1>
<p><%= link_to 'New Product', new_product_path %></p>
<% @products.each do |product| %>
	<section>
	<h3><%= link_to product.title, product_path(product.uuid) %></h3>
	<p><%= product.description %></p>
	<p>
	<%= link_to 'edit', edit_product_path(product.uuid) %>
	<%= link_to 'delete', product_path(product.uuid), method: :delete, data: { confirm: 'Are you sure?' } %> 
	</p>
	<hr>
	</section>
<% end %>